<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ═══ Product Template (Products) ═══ -->
    <record id="action_opencart_product_template" model="ir.actions.act_window">
        <field name="name">Products</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'default_sale_ok': True}</field>
        <field name="domain">[('sale_ok', '=', True)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product
            </p>
            <p>Products with "Can be Sold" enabled will be synced to OpenCart.</p>
        </field>
    </record>

    <!-- ═══ Product Variants ═══ -->
    <record id="action_opencart_product_variant" model="ir.actions.act_window">
        <field name="name">Product Variants</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'default_sale_ok': True}</field>
        <field name="domain">[('sale_ok', '=', True)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product variant
            </p>
        </field>
    </record>

    <!-- ═══ Sale Orders ═══ -->
    <record id="action_opencart_sale_order" model="ir.actions.act_window">
        <field name="name">Orders</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="domain">[('origin', 'like', 'OC-')]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No OpenCart orders imported yet.
            </p>
            <p>Orders imported from OpenCart will have an origin starting with "OC-".</p>
        </field>
    </record>

    <!-- ═══ Customers ═══ -->
    <record id="action_opencart_customers" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="domain">[('customer_rank', '>', 0)]</field>
        <field name="context">{'default_customer_rank': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No customers yet.
            </p>
        </field>
    </record>

</odoo>
