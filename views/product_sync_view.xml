<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_opencart_product_sync_list" model="ir.ui.view">
        <field name="name">opencart.product.sync.list</field>
        <field name="model">opencart.product.sync</field>
        <field name="arch" type="xml">
            <list string="Product Sync" create="false">
                <field name="odoo_product_id"/>
                <field name="opencart_product_id"/>
                <field name="last_sync"/>
                <field name="sync_status"
                       decoration-success="sync_status == 'success'"
                       decoration-danger="sync_status == 'failed'"
                       widget="badge"/>
                <field name="sync_message"/>
            </list>
        </field>
    </record>

    <record id="action_opencart_product_sync" model="ir.actions.act_window">
        <field name="name">Product Sync</field>
        <field name="res_model">opencart.product.sync</field>
        <field name="view_mode">list</field>
    </record>

</odoo>
